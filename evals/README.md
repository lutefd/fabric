# Local V1 Evaluations

`run-local-v1.sh` executes protocol behavior instead of checking only skill
metadata. It covers session discovery, correction capture, cross-thread sync,
budget omission, explicit challenges, causal explanation, JSON capabilities,
conformance, and dry-run write safety.

Run from the repository root:

```bash
./evals/run-local-v1.sh
```
